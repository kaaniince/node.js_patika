const { circleArea, circleCircumference } = require("./circle"); //object destructing

const radius = 5;
console.log("Daire Alanı:", circleArea(radius));
console.log("Daire Çevresi:", circleCircumference(radius));
